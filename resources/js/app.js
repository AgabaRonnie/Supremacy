// resources/js/app.js or front.js

// Load Bootstrap's JavaScript (no jQuery needed for Bootstrap 5)
import 'bootstrap';

// (Optional) Load additional libraries you use globally
// Example: import Chart from 'chart.js/auto';

// Add your custom JS here
document.addEventListener('DOMContentLoaded', () => {
  console.log('App JS loaded');
});
